#ifndef ENUMS_H
#define ENUMS_H


enum RET_NUMS
{
  RET_FAILED = -999, RET_SUCCESS, RET_FULL, RET_DONE,
};

enum FONT_SIZE
{
  XSMALL = 8, SMALL = 10, MEDIUM = 12, LARGE = 14, XLARGE = 16
};

enum TEXTURE_ID
{
  NONE = 100, TEST, ALPHA, BACK, IN_FILE,
};

enum SCENE
{
  MAIN_MENU = 1000, TEXTURE_EDITOR, COMBAT,
};

enum CONSOLE_COMMAND
{
  UNKNOW = -9999, EXIT,
};

#endif //ENUMS_H
